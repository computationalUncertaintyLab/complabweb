/* Kept for compatibility; bubble animation is now CSS-only on load. */
(function () {
  const mark = document.getElementById("home-splash-mark");
  if (mark) mark.setAttribute("data-bubbles", "css");
})();
